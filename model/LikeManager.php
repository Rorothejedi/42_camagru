<?php
namespace App\model;

/**
 * Permet de gérer les requêtes concernant les likes.
 */
class LikeManager 
{
	/**
	 * Permet de compter le nombre de like par image.
	 */
	// public function getNbrLikes(Image $image)
	// {
	// 	$data = App::getDb()->prepare('
	// 		SELECT *
	// 		FROM `like`
	// 		WHERE id_image = :id_image',
	// 		['id_image' => $image->id()],
	// 	true, false, true);
	// 	return $data;
	// }
}